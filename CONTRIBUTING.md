# Contributing

Keep changes focused and preserve the public interface contract. Add a
Common Test case through `enats_client` for every behaviour change. Run
`make static_checks` and `make coverage` before submitting a pull request.
Use `erlfmt` for Erlang formatting and explain compatibility or protocol
decisions in the commit message.
